<%= content_for :page_title, "Add details for #{@humanized_event_name}" %>
<%= content_for :before_content, govuk_back_link_to(timetables_path) %>

<p class="govuk-heading-m"><%= @humanized_event_name %></p>
